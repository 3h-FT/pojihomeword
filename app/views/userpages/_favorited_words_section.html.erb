<div class=" bg-pink-50 py-4 px-4">
  <h3 class="text-center text-2xl sm:text-3xl font-semibold text-pink-400 py-4">
    お気に入り登録: <span class="text-red-600"><%= @favorited_words.count %></span> 個
  </h3>
  <div class="space-y-2 mb-8">
<%= render partial: 'ai_messages/result', collection: @favorited_words_page, as: :positive_word,
           locals: {
             show_header_and_button: false,
             favorited_word_ids: @favorited_word_ids,
             from: 'userpages' # ← これが大事
           } %>
  </div>
</div>
