<div id="positive-word-id-<%= @positive_word.id %>">
  <div class="max-w-3xl mx-auto bg-white p-6 rounded-lg shadow-lg">
    <div class="p-6">
      <h3 class="text-2xl font-semibold text-red-400 text-center mb-6">ポジティブワード</h3>

  <% if @show_edit_form && user_signed_in? %>
    <%= render partial: "edit_form", locals: { positive_word: @positive_word } %>
  <% else %>
    <%= render partial: "word_updata", locals: { positive_word: @positive_word } %>
  <% end %>

    </div>        
  </div>
</div>