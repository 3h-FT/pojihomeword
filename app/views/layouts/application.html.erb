<!DOCTYPE html>
<html lang="ja" class="h-full bg-white">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for(:title) ? "#{content_for(:title)} | ポジほめワード" : "ポジほめワード" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= tag.link rel: "stylesheet", href: "https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&display=swap" %>
    <%= tag.link rel: "stylesheet", href: "https://fonts.googleapis.com/css2?family=Roboto&display=swap" %>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      font-family: 'M PLUS Rounded 1c', sans-serif;
    }

    .font-not-rounded {
      font-family: 'Roboto', sans-serif;
    }
  </style>

    <%= yield :head %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full overflow-x-hidden text-gray-800">
    <%= render 'shared/header' %>

    <main class="min-h-screen">
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
