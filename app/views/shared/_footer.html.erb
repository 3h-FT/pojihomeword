<footer class="fixed bottom-0 w-full bg-pink-50 px-4 sm:px-6 lg:px-8 py-2 shadow-inner border-t border-pink-200 z-50">
  <div class="flex">
    <!-- メニュー1 -->
    <%= link_to new_ai_message_path, class: "w-1/3 flex flex-col items-center justify-center text-pink-700 hover:text-pink-800" do %>
      <div class="h-10 w-10 rounded-full bg-pink-200 flex items-center justify-center mb-1">
        <i class="bi bi-lightbulb text-lg"></i>
      </div>
      <span class="text-xs">知る</span>
    <% end %>

    <!-- メニュー2 -->
    <%= link_to userpages_path, class: "w-1/3 flex flex-col items-center justify-center border-l border-gray-300 text-yellow-600 hover:text-yellow-800" do %>
      <div class="h-10 w-10 rounded-full bg-yellow-200 flex items-center justify-center mb-1">
        <i class="bi bi-journals text-lg"></i>
      </div>
      <span class="text-xs">ためる</span>
    <% end %>

    <!-- メニュー3 -->
    <%= link_to posts_path, class: "w-1/3 flex flex-col items-center justify-center border-l border-gray-300 text-red-600 hover:text-red-800" do %>
      <div class="h-10 w-10 rounded-full bg-red-200 flex items-center justify-center mb-1">
        <i class="bi bi-share-fill text-lg"></i>
      </div>
      <span class="text-xs">共有する</span>
    <% end %>
  </div>
</footer>
