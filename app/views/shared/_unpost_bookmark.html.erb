<% if (fav = current_user.post_favorites.find_by(post_id: post.id)) %>
  <%= link_to post_favorite_path(fav.id),
              data: { turbo_method: :delete },
              class: "text-mg text-yellow-500 hover:underline", title: "いいね解除" do %>
    <i class="bi bi-star-fill text-left text-sm"></i>いいね
  <% end %>
<% end %>